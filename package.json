{
  "private": true,
  "name": "{{ project-name }}",
  "version": "1.0.0",
  "description": "Dart hello world for Cloudflare Workers",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "{{ authors }}",
  "license": "MIT",
  "devDependencies": {}
}
